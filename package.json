{
  "name": "my-avatars",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "test:unit": "node --test tests/unit/*.test.js",
    "test:integration": "node --test tests/integration/*.test.js",
    "serve:test": "node tests/support/static-server.mjs --port 8000"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.4"
  }
}
